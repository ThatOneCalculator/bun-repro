{
    "name": "backend",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "bun --watch src/index.ts",
        "dev:works": "bun --env-file=../../.env.development --watch src/index.ts"
    }
}